{
  "name": "contracts",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "deploy": "npm run clean && npm run compile && hardhat ignition deploy ignition/modules/PropertyTokenization.ts --network etherlinkTestnet",
    "compile": "hardhat compile",
    "clean": "hardhat clean"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ignition": "^3.0.0",
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.0",
    "@types/node": "^22.18.0",
    "forge-std": "github:foundry-rs/forge-std#v1.9.4",
    "hardhat": "^3.0.1",
    "typescript": "~5.8.0",
    "viem": "^2.35.1"
  }
}
